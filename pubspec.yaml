name: sirah
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.1+2022042901

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  intl: ^0.17.0
  flutter_markdown: ^0.6.9+1
  firebase_analytics: ^9.1.3
  firebase_core: ^1.14.0
  flutter_bloc: ^8.0.1
  get_it: ^7.2.0
  equatable: ^2.0.3
  dio_http_cache: ^0.3.0
  dio: ^4.0.6
  dartz: ^0.10.1

dev_dependencies:
  flutter_launcher_icons: ^0.9.2
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/search_icon.png
    - assets/flutter_logo.png
    - assets/right_arrow.png
    - assets/articles/

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/sirah.png"